{
  "name": "where-money-firestore-rules",
  "private": true,
  "type": "module",
  "description": "Runs firestore.rules against the emulator. Not part of the Dart workspace.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-where-money --config ../../firebase.json \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
